记录所有修改内容

| 修改位置                      | 修改内容                       | 解释                                                   |
| ----------------------------- | ------------------------------ | ------------------------------------------------------ |
| 头文件类声明                  | 修改声明顺序                   | 按照 公开->保护->私有，成员变量->函数，繁->简 顺序声明 |
| 头文件类所有成员变量声明      | 修改为私有，修改为小写＋下滑线 | 按照谷歌规范                                           |
| 头文件类函数GetADCValue()声明 | 修改为adc_value()              | 按照谷歌规范                                           |
| 源文件using命名空间           | 改为namespace 命名空间{ ... }  | 按照谷歌规范                                           |
| 源文件函数定义                | 配合头文件修改                 | -                                                      |

额外添加

```c
#if defined(HAL_ADC_MODULE_ENABLED)
#ifndef __BSP_ADC_HPP
#define __BSP_ADC_HPP

#include "adc.h"
#include <stdint.h>
```

## 公开修改

此修改需要在其他上层文件中同步

| 修改位置                                                  | 修改内容                                              | 解释 |
| --------------------------------------------------------- | ----------------------------------------------------- | ---- |
| namespace Bsp_ADC_n                                       | namespace Bsp_n                                       |      |
| class Bsp_ADC_c                                           | class ADC_c                                           |      |
| Bsp_ADC_c( ADC_HandleTypeDef *hadc ,uint16_t adc_length); | ADC_c( ADC_HandleTypeDef *hadc ,uint16_t adc_length); |      |
| void ADC_Start(void);                                     | void Start(void);                                     |      |
| void ADC_Stop(void);                                      | void Stop(void);                                      |      |
|                                                           |                                                       |      |
|                                                           |                                                       |      |
|                                                           |                                                       |      |

